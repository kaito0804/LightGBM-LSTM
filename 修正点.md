
# 1. 正しいバックテストの提出（最重要）
「simple_backtest.pyの中身を見ましたが、これでは本番のAIモデル（LightGBM+LSTM）の性能テストになっていません。
main.pyで使用しているのと同じAIモデル、同じロジックを使って、過去1年分のバックテストを行い、その資産推移グラフ（PF:プロフィットファクター含む）を見せてください。」

# 2. リアルタイム性の向上（WebSocket化）
「デイトレードで板情報（Imbalance）を見ているのに、HTTPリクエスト（ポーリング）で60秒ごとの取得では遅すぎて使い物にならないと思います。
HyperliquidのWebSocket APIを使用して、リアルタイムに価格と板情報を監視し、チャンスを逃さないように改修してください。」

# 3. MLモデルの学習プロセスの開示
「ml_predictor.pyでモデルを定義していますが、実際にどのデータを教師データとして学習させたのか、その特徴量の重要度（Feature Importance）はどうなっているのかレポートをください。
単に価格とRSIを入れただけでは勝てないはずです。」

# 4. 具体的なリカバリー策の実装
「『大暴落時に損失を抑え、1週間で取り戻す』という要件に対し、コード上では損切り設定しか見当たりません。
ドローダウン（資産減少）が起きた後、ロットサイズを自動で調整して安全に復帰する『ドローダウン・リカバリー機能』を具体的にどう実装したのか説明してください。」